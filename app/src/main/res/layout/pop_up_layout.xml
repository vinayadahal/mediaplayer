<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="#c0000000"
    android:orientation="vertical">

    <RelativeLayout
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:background="#a0000000">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize">

            <ImageView
                android:id="@+id/phone_icon"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerVertical="true"
                android:padding="10dp"
                android:src="@drawable/play_circle_outline" />

            <TextView
                android:id="@+id/title_text"
                android:layout_width="wrap_content"
                android:layout_height="?attr/actionBarSize"
                android:gravity="center"
                android:paddingLeft="45dp"
                android:text="Resume Video Playback"
                android:textColor="#fff" />

        </RelativeLayout>

        <RelativeLayout
            android:id="@+id/popup_lnrLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="?attr/actionBarSize"
            android:orientation="vertical">

            <TextView
                android:id="@+id/resume_text"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="10dp"
                android:text="Do you want to resume playback from where you left?"
                android:textColor="#fff" />

            <Button
                android:id="@+id/popup_call_btn"
                android:layout_width="150dp"
                android:layout_height="?attr/actionBarSize"
                android:layout_alignParentLeft="true"
                android:layout_below="@id/resume_text"
                android:background="@drawable/btn_styler_playback"
                android:text="RESUME"
                android:textColor="#fff" />

            <Button
                android:id="@+id/popup_sms_btn"
                android:layout_width="150dp"
                android:layout_height="?attr/actionBarSize"
                android:layout_alignParentRight="true"
                android:layout_below="@id/resume_text"
                android:background="@drawable/btn_styler_playback"
                android:text="CANCEL"
                android:textColor="#fff" />

        </RelativeLayout>

    </RelativeLayout>

</RelativeLayout>
